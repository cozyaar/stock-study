{
    "name": "stock-study-monorepo",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vercel dev",
        "build": "npm --prefix app install && npm --prefix app run build"
    },
    "dependencies": {
        "axios": "^1.6.0",
        "csv-parser": "^3.0.0",
        "yahoo-finance2": "^3.13.0"
    },
    "devDependencies": {
        "@vercel/node": "^5.0.0",
        "typescript": "^5.7.3"
    }
}